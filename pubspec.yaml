name: building_a_responsive_dashboard_ui_app_with_flutter_step_by_step_tutorial_by_heyflutter_com
description: "Building a Responsive Dashboard UI App with Flutter | Step-by-Step Tutorial"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
